#include <stdio.h>

/**
 * main - function that prints the alphabet in lower case
 *
 * Return: Always (Success)
 */

int main(void)
{
int i;
for (i = 'a'; i <= 'z'; i++)
{
putchar(i);
}
putchar('\n');
}
